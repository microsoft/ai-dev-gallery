<?xml version="1.0" encoding="utf-8" ?>
<samples:BaseSamplePage
    x:Class="AIDevGallery.Samples.WCRAPIs.ColoringBook"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:samples="using:AIDevGallery.Samples"
    mc:Ignorable="d">
    <Grid ColumnSpacing="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MaxWidth="600" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Grid.ColumnSpan="2">
            <TextBox x:Name="InputTextBox" Margin="0,0,100,0"
                     AutomationProperties.Name="Prompt input"
                     Description=" "
                     PlaceholderText="Enter a prompt to generate image.."
                     Text="Red cup"
                     TextChanged="InputBox_Changed" />
            <Button HorizontalAlignment="Right" Margin="0,0,0,18"
                    x:Name="ChangeButton"
                    Click="ChangeButton_Click"
                    Content="Change"
                    IsEnabled="False"
                    Style="{ThemeResource AccentButtonStyle}" />
            <Button HorizontalAlignment="Right" Margin="0,0,0,18"
                    x:Name="StopBtn"
                    MinWidth="86"
                    AutomationProperties.Name="Stop"
                    Click="StopBtn_Click"
                    Style="{ThemeResource AccentButtonStyle}"
                    Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon
                            x:Name="StopIcon"
                            FontSize="16"
                            Glyph="&#xE73B;" />
                    <ProgressRing
                            x:Name="OutputProgressBar"
                            Width="16"
                            Height="16"
                            Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                            IsIndeterminate="True"
                            Visibility="Collapsed" />
                    <TextBlock Text="Stop" />
                </StackPanel>
            </Button>
        </Grid>
        <Grid Grid.Row="1"
            MinWidth="240"
            MaxWidth="600"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
            BorderThickness="2"
            CornerRadius="{StaticResource OverlayCornerRadius}">

            <Grid CornerRadius="{ThemeResource OverlayCornerRadius}">
                <Image
                        x:Name="CanvasImage"
                        PointerReleased="CanvasImage_PointerReleased" 
                        SizeChanged="CanvasImage_SizeChanged"
                        VerticalAlignment="Stretch"
                        Stretch="Uniform"
                        MaxHeight="400"/>
                <Canvas x:Name="DrawCanvas" IsHitTestVisible="False"/>
            </Grid>

            <Grid
                x:Name="RevertButton"
                Margin="8"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                CornerRadius="{StaticResource ControlCornerRadius}"
                Visibility="Collapsed">
                <Button
                    AutomationProperties.Name="Revert"
                    Click="RevertButton_Click"
                    ToolTipService.ToolTip="Undo erase">
                    <FontIcon FontSize="16" Glyph="&#xE7A7;" />
                </Button>
            </Grid>
            <Grid
                Margin="8"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                CornerRadius="{StaticResource ControlCornerRadius}">
                <DropDownButton
                    Padding="8"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    AutomationProperties.Name="Select image"
                    ToolTipService.ToolTip="Select image">
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Click="LoadImage_Click" Text="From file" />
                            <MenuFlyoutItem Click="PasteImage_Click" Text="From clipboard" />
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                    <FontIcon FontSize="16" Glyph="&#xEE71;" />
                </DropDownButton>
            </Grid>
        </Grid>
        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            MaxWidth="168"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Orientation="Vertical"
            Spacing="8">

            <Button
                x:Name="ClearSelectionButton"
                Click="ClearSelectionButton_Click"
                Content="Clear selection"
                HorizontalAlignment="Right"
                IsEnabled="False" />
            <TextBlock
                x:Name="InstructionText"
                Margin="0,12,0,0"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="Click where you want to color, add a prompt and click 'Change'."
                TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</samples:BaseSamplePage>
