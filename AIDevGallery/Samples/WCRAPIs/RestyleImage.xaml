<?xml version="1.0" encoding="utf-8" ?>
<samples:BaseSamplePage
    x:Class="AIDevGallery.Samples.WCRAPIs.RestyleImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:samples="using:AIDevGallery.Samples"
    mc:Ignorable="d">
    <Grid ColumnSpacing="8" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="InputTextBox" 
                     AutomationProperties.Name="Prompt input"
                     Description=" "
                     KeyUp="TextBox_KeyUp"
                     PlaceholderText="Enter a prompt to generate image.."
                     Text="Make the image as painting, Van Gogh style"
                     TextChanged="InputBox_Changed" />
        <StackPanel
            Grid.Column="1"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                x:Name="GenerateButton"
                MinWidth="86"
                Click="GenerateButton_Click"
                Content="Generate"
                Style="{ThemeResource AccentButtonStyle}" />
            <Button
                x:Name="StopBtn"
                MinWidth="86"
                AutomationProperties.Name="Stop"
                Click="StopBtn_Click"
                Style="{ThemeResource AccentButtonStyle}"
                Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon
                        x:Name="StopIcon"
                        FontSize="16"
                        Glyph="&#xE73B;" />
                    <ProgressRing
                        x:Name="OutputProgressBar"
                        Width="16"
                        Height="16"
                        Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}"
                        IsIndeterminate="True"
                        Visibility="Collapsed" />
                    <TextBlock Text="Stop" />
                </StackPanel>
            </Button>
        </StackPanel>
        <Grid Grid.Row="1" Grid.ColumnSpan="2" ColumnSpacing="5" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image x:Name="InputImage" 
                   Grid.Column="0" 
                   Stretch="Uniform"
                   MaxHeight="400" 
                   HorizontalAlignment="Left" 
                   VerticalAlignment="Top"/>
            <StackPanel Grid.Column="0" Margin="8" HorizontalAlignment="Right"
                        VerticalAlignment="Top" 
                        Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}" 
                        CornerRadius="{StaticResource ControlCornerRadius}">
                <DropDownButton Padding="8" 
                                HorizontalAlignment="Right" 
                                VerticalAlignment="Top" 
                                AutomationProperties.Name="Select image" 
                                ToolTipService.ToolTip="Select image">
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Click="LoadImage_Click" Text="From file" />
                            <MenuFlyoutItem Click="PasteImage_Click" Text="From clipboard" />
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                    <FontIcon FontSize="16" Glyph="&#xEE71;" />
                </DropDownButton>
            </StackPanel>
            <Image x:Name="GeneratedImage" 
               Grid.Column="1"
               AutomationProperties.LiveSetting="Assertive"
               AutomationProperties.Name="Output"
               HorizontalAlignment="Left" 
               VerticalAlignment="Top" 
               Stretch="Uniform"
               MaxHeight="400"/>
            <StackPanel
                Margin="12"
                Padding="8"
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                CornerRadius="{StaticResource ControlCornerRadius}"
                Orientation="Horizontal"
                Spacing="8">
                <Button x:Name="CopyButton"
                    AutomationProperties.Name="Copy Image to the Clipboard"
                    Click="Copy_Click"
                    ToolTipService.ToolTip="Copy Image to the Clipboard"
                    Visibility="Collapsed">
                    <FontIcon FontSize="16" Glyph="&#xE8C8;" />
                </Button>
                <Button
                    x:Name="SaveButton"
                    Click="Save_Click"
                    ToolTipService.ToolTip="Save Image"
                    Visibility="Collapsed">
                    <FontIcon FontSize="16" Glyph="&#xEE71;" />
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</samples:BaseSamplePage>
