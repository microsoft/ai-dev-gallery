<?xml version="1.0" encoding="utf-8" ?>
<samples:BaseSamplePage
    xmlns:samples="using:AIDevGallery.Samples"
    x:Class="AIDevGallery.Samples.WCRAPIs.OCRLineSample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:AIDevGallery.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls" mc:Ignorable="d">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="LoadButton" Click="LoadImage_Click" Margin="10"
                    Style="{StaticResource AccentButtonStyle}">Load Image</Button>
            <Button x:Name="PasteButton" Click="PasteImage_Click" Margin="10"
                    Style="{StaticResource AccentButtonStyle}">Paste Image</Button>
            <Button x:Name="CopyTextButton" Click="CopyText_Click" Margin="10"
                    Style="{StaticResource AccentButtonStyle}">Copy all text</Button>
        </StackPanel>
        <TextBlock Text="Click on the text to copy to clipboard" Grid.Row="1" Margin="10" />
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Viewbox x:Name="ImageViewbox" Grid.Column="0" HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
                <Grid x:Name="PaneGrid" HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch">
                    <Image x:Name="ImageSrc" Stretch="Uniform" />
                    <Canvas x:Name="RectCanvas" Visibility="Collapsed" />
                </Grid>
            </Viewbox>
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Auto" Grid.Column="1" Margin="10"
                    HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <StackPanel x:Name="TextPanel">
                    <StackPanel.ContextFlyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="Copy All" Click="CopyText_Click" />
                        </MenuFlyout>
                    </StackPanel.ContextFlyout>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</samples:BaseSamplePage>
