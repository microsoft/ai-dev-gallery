<?xml version="1.0" encoding="utf-8" ?>
<samples:BaseSamplePage
    x:Class="AIDevGallery.Samples.WCRAPIs.SemanticSearch"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:AIDevGallery.Samples.WCRAPIs"
    xmlns:samples="using:AIDevGallery.Samples"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Spacing="8">
            <TextBlock 
                Style="{StaticResource CaptionTextBlockStyle}" 
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                Text="Sample text data:"
                Margin="0"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <controls:ItemsView
                    x:Name="textDataItemsView"
                    ItemsSource="{x:Bind TextDataItems}"
                    SelectionMode="None"
                    IsItemInvokedEnabled="False"
                    Grid.Column="0"
                    Padding="0,0,0,12">
                    <controls:ItemsView.ItemTemplate>
                        <DataTemplate x:DataType="local:TextDataItem">
                            <controls:ItemContainer AutomationProperties.Name="{x:Bind Id}" Width="200" Margin="3">
                                <Grid>
                                    <TextBox Text="{x:Bind Value, Mode=TwoWay}"
                                         Tag="{x:Bind Id}"
                                         TextChanged="SemanticTextBox_TextChanged"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         Padding="11,32,11,11"
                                         Height="120"/>
                                    <Button
                                        x:Name="textCloseButton"
                                        Click="closeButton_Click"
                                        Tag="{x:Bind}"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="0,4,4,0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Style="{StaticResource SubtleButtonStyle}">
                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE711;" FontSize="16"/>
                                    </Button>

                                    <TextBlock Text="{x:Bind Id}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,7" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                </Grid>
                            </controls:ItemContainer>
                        </DataTemplate>
                    </controls:ItemsView.ItemTemplate>
                    <controls:ItemsView.Layout>
                        <StackLayout Orientation="Horizontal" />
                    </controls:ItemsView.Layout>
                </controls:ItemsView>
                <Button
                    Grid.Column="1"
                    VerticalAlignment="Top"
                    Margin="12,0,0,0"
                    Click="AddTextDataButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE898;" FontSize="16" Margin="0,0,4,0"/>
                        <TextBlock Text="Add Text Data"/>
                    </StackPanel>
                </Button>
            </Grid>

            <TextBlock Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Sample image data:" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <controls:ItemsView
                    x:Name="ImageDataItemsView"
                    ItemsSource="{x:Bind ImageDataItems}"
                    SelectionMode="None"
                    IsItemInvokedEnabled="False"
                    Grid.Column="0">
                    <controls:ItemsView.ItemTemplate>
                        <DataTemplate x:DataType="local:ImageDataItem">
                            <controls:ItemContainer AutomationProperties.Name="{x:Bind Id}" Width="200" Margin="3">
                                <Grid>
                                    <Image Source="{x:Bind ImageSource}"
                                           Tag="{x:Bind Id}"
                                           ImageOpened="ImageData_ImageOpened" 
                                           Stretch="Fill" 
                                           HorizontalAlignment="Stretch" 
                                           VerticalAlignment="Stretch" 
                                           MinWidth="70"/>
                                    <Border Background="{ThemeResource LayerFillColorAltBrush}" Height="34" VerticalAlignment="Top" />
                                    <Button
                                        x:Name="imageCloseButton"
                                        Click="closeButton_Click"
                                        Tag="{x:Bind}"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Margin="0,4,4,0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Style="{StaticResource SubtleButtonStyle}">
                                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE711;" FontSize="16"/>
                                    </Button>

                                    <TextBlock Text="{x:Bind Id}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12,7" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                </Grid>
                            </controls:ItemContainer>
                        </DataTemplate>
                    </controls:ItemsView.ItemTemplate>
                    <controls:ItemsView.Layout>
                        <StackLayout Orientation="Horizontal" />
                    </controls:ItemsView.Layout>
                </controls:ItemsView>
                <Button
                    Grid.Column="1"
                    VerticalAlignment="Top"
                    Margin="12,0,0,0"
                    Click="UploadImageButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE898;" FontSize="16" Margin="0,0,4,0"/>
                        <TextBlock Text="Upload Image"/>
                    </StackPanel>
                </Button>
            </Grid>
            
            <TextBox
                 x:Name="SearchTextBox"
                 AutomationProperties.Name="Prompt input"
                 PlaceholderText="Enter a word or phrase to search for"
                 TextWrapping="Wrap" />
            <ProgressBar
                x:Name="OutputProgressBar"
                    VerticalAlignment="Bottom"
                    IsIndeterminate="True"
                    Visibility="Collapsed" />
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <InfoBar
                        x:Name="IndexingMessage"
                        IsOpen="False"
                        Severity="Informational"
                        Title="Indexing"
                        Message="Partial results may be available." 
                        Padding="0"/>
                    <InfoBar
                        x:Name="RemovedItemMessage"
                        IsOpen="False"
                        Severity="Informational"
                        Message="Item has been removed" 
                        Padding="0"/>
                </StackPanel>

                <StackPanel Orientation="Vertical" 
                        Grid.Column="1">
                    <Button
                        x:Name="SearchButton"
                        HorizontalAlignment="Right"
                        Click="SearchButton_Click"
                        Content="Search"
                        Margin="10,0,0,0"
                        Style="{StaticResource AccentButtonStyle}" />

                    <AppBarButton AllowFocusOnInteraction="True" Icon="Edit" Label="Edit">
                        <AppBarButton.Flyout>
                            <Flyout>
                                <StackPanel>
                                    <!-- Query Options -->
                                    <TextBlock Text="Query Options" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                    <TextBox x:Name="QueryLanguageTextBox" Header="Query Language" PlaceholderText="e.g., en-US"/>

                                    <!--Text Match Options-->
                                    <TextBlock Text="Text Match Options" FontWeight="SemiBold" Margin="0,12,0,4"/>
                                    <ComboBox x:Name="TextMatchScopeComboBox" Header="Match Scope" HorizontalAlignment="Stretch" SelectedIndex="0">
                                        <ComboBoxItem Content="Unconstrained"/>
                                        <ComboBoxItem Content="Region"/>
                                        <ComboBoxItem Content="ContentItem"/>
                                    </ComboBox>
                                    <ComboBox x:Name="TextMatchTypeComboBox" Header="Match Type" HorizontalAlignment="Stretch" SelectedIndex="0">
                                        <ComboBoxItem Content="Fuzzy"/>
                                        <ComboBoxItem Content="Exact"/>
                                    </ComboBox>

                                    <!--Image Match Options-->
                                    <TextBlock Text="Image Match Options" FontWeight="SemiBold" Margin="0,12,0,4"/>
                                    <ComboBox x:Name="ImageMatchScopeComboBox" Header="Match Scope" HorizontalAlignment="Stretch" SelectedIndex="0">
                                        <ComboBoxItem Content="Unconstrained"/>
                                        <ComboBoxItem Content="Region"/>
                                        <ComboBoxItem Content="ContentItem"/>
                                    </ComboBox>
                                    <ComboBox x:Name="ImageOcrTextMatchTypeComboBox" Header="OCR Match Type" HorizontalAlignment="Stretch" SelectedIndex="0">
                                        <ComboBoxItem Content="Fuzzy"/>
                                        <ComboBoxItem Content="Exact"/>
                                    </ComboBox>
                                </StackPanel>
                            </Flyout>
                        </AppBarButton.Flyout>
                    </AppBarButton>
                </StackPanel>
            </Grid>
        </StackPanel>
        <Grid
            x:Name="ResultsGrid"
            Grid.Row="2"
            Visibility="Collapsed">
            <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}">Search Results:</TextBlock>
            <ScrollViewer
                Margin="0,25,0,0"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Visible">
                <TextBlock
                    x:Name="ResultsTextBlock"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    IsTextSelectionEnabled="True"
                    TextWrapping="WrapWholeWords" />
            </ScrollViewer>
            <ItemsControl x:Name="ImageResultsBox" Grid.Row="2" Margin="0,10,0,0" Visibility="Collapsed">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapGrid Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding}" Height="100" Width="100" Margin="5"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Grid>
</samples:BaseSamplePage>
