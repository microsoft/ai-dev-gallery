<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AIDevGallery.Pages.FileOperationsMCPPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1,0,0,0">
        <ScrollViewer>
            <StackPanel Margin="48,32,48,48" Spacing="24">
                <TextBlock
                    Style="{StaticResource TitleTextBlockStyle}"
                    Text="File Operations MCP" />
                
                <TextBlock
                    Style="{StaticResource BodyTextBlockStyle}"
                    TextWrapping="Wrap">
                    This demo shows how to interact with the built-in Windows File MCP Server using the Model Context Protocol.
                    The File MCP Server provides tools to perform file operations on Windows.
                </TextBlock>

                <InfoBar
                    x:Name="PrerequisiteInfoBar"
                    IsOpen="True"
                    Severity="Informational"
                    Title="Prerequisites"
                    Message="This demo requires Windows MCP Private Preview 3 or later with odr.exe available. The File MCP Server must be enabled in Settings > System > AI Components." />

                <StackPanel Spacing="12">
                    <TextBlock
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="Connection Status" />
                    
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Button
                            x:Name="ConnectButton"
                            Content="Connect to File Server"
                            Click="ConnectButton_Click"
                            Style="{StaticResource AccentButtonStyle}" />
                        
                        <Button
                            x:Name="DisconnectButton"
                            Content="Disconnect"
                            Click="DisconnectButton_Click"
                            IsEnabled="False" />
                        
                        <ProgressRing
                            x:Name="ConnectionProgressRing"
                            Width="20"
                            Height="20"
                            IsActive="False" />
                        
                        <TextBlock
                            x:Name="ConnectionStatusText"
                            VerticalAlignment="Center"
                            Text="Not connected" />
                    </StackPanel>
                </StackPanel>

                <Grid x:Name="ToolsPanel" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="12">
                        <TextBlock
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            Text="Available Tools" />
                        
                        <TextBlock
                            Style="{StaticResource CaptionTextBlockStyle}"
                            TextWrapping="Wrap"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            Select a tool below to execute it. File operation tools may require parameters such as file paths.
                        </TextBlock>
                        
                        <ScrollViewer MaxHeight="600" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="ToolsList" />
                        </ScrollViewer>
                    </StackPanel>

                    <StackPanel x:Name="ResultPanel" Grid.Column="1" Spacing="12" Margin="24,0,0,0" Visibility="Collapsed">
                        <TextBlock
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            Text="Results" />
                        
                        <Border
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16">
                            <ScrollViewer MaxHeight="600">
                                <TextBlock
                                    x:Name="ResultTextBlock"
                                    FontFamily="Consolas"
                                    IsTextSelectionEnabled="True"
                                    TextWrapping="Wrap" />
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </Grid>

                <StackPanel x:Name="ErrorPanel" Spacing="12" Visibility="Collapsed">
                    <InfoBar
                        x:Name="ErrorInfoBar"
                        Severity="Error"
                        IsOpen="True"
                        Title="Error" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
