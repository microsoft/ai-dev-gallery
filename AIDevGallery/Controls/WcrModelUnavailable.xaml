<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="AIDevGallery.Controls.WcrModelUnavailable"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <StackPanel
        Margin="0"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Orientation="Vertical"
        Spacing="8">
        <Image Width="36" Source="ms-appx:///Assets/ModelIcons/WCRAPI.svg" />
        <TextBlock
            FontWeight="SemiBold"
            Text="{x:Bind Title, Mode=OneWay}"
            TextAlignment="Center"
            TextWrapping="Wrap" />
        <TextBlock
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            IsTextSelectionEnabled="True"
            TextAlignment="Center"
            TextWrapping="Wrap">
            <Run Text="{x:Bind Message, Mode=OneWay}" />
            <LineBreak />
            <Hyperlink NavigateUri="{x:Bind LearnMoreUri, Mode=OneWay}" UnderlineStyle="None">
                <Run Text="{x:Bind LearnMoreText, Mode=OneWay}" />
            </Hyperlink>
            <LineBreak />
            <LineBreak />
            <Run Text="You can still view the code and export the sample." />
        </TextBlock>
    </StackPanel>
</UserControl>


